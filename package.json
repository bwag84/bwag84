{
  "name": "canvas",
  "version": "1.0.0",
  "description": "Personal portfolio site built with Hugo and CareerCanvas theme",
  "scripts": {
    "dev": "hugo server -D",
    "build": "hugo --gc --minify",
    "build:css": "tailwindcss -i ./assets/css/main.css -o ./static/css/main.css --minify"
  },
  "devDependencies": {
    "@tailwindcss/typography": "^0.5.10",
    "autoprefixer": "^10.4.16",
    "postcss": "^8.4.32",
    "postcss-cli": "^11.0.0",
    "tailwindcss": "^3.3.6"
  }
}
